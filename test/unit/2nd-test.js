import { describe, it } from 'mocha';
import assert from 'assert';
import { Storage } from '../../src/common/storage';

describe('2ndTest', function() {
    it('some other test', function() {
        assert.equal(true, true);
    });
});
