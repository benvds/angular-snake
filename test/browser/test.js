// import { angular } from 'angular-mocks';
import { describe, it } from 'mocha';
import assert from 'assert';
// import { Storage } from '../src/common/storage';

describe('Other', function() {
    it('asserts booleans', function() {
        assert.equal(true, true);
    });
});
